'use client';import {useEffect,useState} from 'react';import Link from 'next/link';
export function NoticeList(){const [notices,setNotices]=useState<any[]>([]);useEffect(()=>{fetch('/backend/api/v1/public/circulars').then(r=>r.json()).then(setNotices)},[]);return <div className="card"><div className="card-body"><div className="split-head"><h3 style={{margin:0}}>🔔 গ্রিড সার্কুলার ও অফিসিয়াল বিজ্ঞপ্তি</h3><Link href="/circulars" className="green-text">↗</Link></div>{notices.slice(0,4).map(x=><div key={x.id} className="notice-mini"><div className="meta-row"><span className="tag green">{x.category}</span><span className="muted">{x.published_at?new Date(x.published_at).toLocaleDateString('bn-BD'):''}</span></div><strong>{x.title_bn}</strong></div>)}</div></div>}
