'use client';import {SectionHead} from '@/components/SectionHead';import {useEffect,useState} from 'react';
export default function Media(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/backend/api/v1/public/media').then(r=>r.json()).then(setItems)},[]);return <section className="section dark"><div className="container"><SectionHead dark eyebrow="IDEB MEDIA & EVENTS" title="মিডিয়া গ্যালারি" subtitle="ছবি, ভিডিও, সম্মেলন ও সাংগঠনিক কার্যক্রম।"/><div className="grid-3">{items.map(x=><article key={x.id} className="media-card"><div className="media-image">▧</div><div className="media-card-body"><span className="tag dark-tag">{x.media_type}</span><h3>{x.title_bn}</h3><p>{x.description_bn}</p></div></article>)}</div></div></section>}
