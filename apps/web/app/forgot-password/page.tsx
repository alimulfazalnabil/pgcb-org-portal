'use client';
import {useState} from 'react';
export default function ForgotPassword(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[token,setToken]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/backend/api/v1/auth/password-reset/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json().catch(()=>({}));if(r.ok){setMsg(d.message);if(d.reset_token)setToken(d.reset_token)}else setMsg(d.detail||'Request failed');setBusy(false)}return <section className="section"><div className="container narrow"><div className="card card-body"><span className="eyebrow">ACCOUNT RECOVERY</span><h1>পাসওয়ার্ড রিসেট</h1><p className="muted">আপনার অ্যাকাউন্টের ইমেইল দিন।</p><form className="form-stack" onSubmit={submit}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.org"/><button className="btn btn-primary" disabled={busy}>{busy?'প্রসেসিং...':'Reset password'}</button></form>{msg&&<div className="notice-success">{msg}</div>}{token&&<div style={{marginTop:14}}><p className="muted small">Development token (production-এ ইমেইল লিংকে পাঠানো হবে):</p><code style={{wordBreak:'break-all'}}>{token}</code><p><a className="btn btn-light" href={`/reset-password?token=${encodeURIComponent(token)}`}>Reset now</a></p></div>}</div></div></section>}
