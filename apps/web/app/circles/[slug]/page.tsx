import { notFound } from 'next/navigation';
const allowed=['ঢাকা','চট্টগ্রাম','কুমিল্লা','সিলেট','খুলনা','রাজশাহী','রংপুর','ময়মনসিংহ','বরিশাল'];
export default async function Circle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=decodeURIComponent(slug);if(!allowed.includes(name))notFound();return <section className="section"><div className="container"><div className="card card-body"><span style={{color:'var(--blue)',fontWeight:900}}>PGCB GRID CIRCLE</span><h1>{name} গ্রিড সার্কেল</h1><p style={{color:'var(--muted)'}}>সার্কেল সভাপতি, সাধারণ সম্পাদক, সদস্য তালিকা এবং স্থানীয় কার্যক্রমের তথ্য।</p></div><div className="grid-3" style={{marginTop:20}}>{['সভাপতি','সাধারণ সম্পাদক','কোষাধ্যক্ষ'].map((x,i)=><div key={x} className="card card-body"><div style={{fontSize:60}}>👤</div><h3>{x}</h3><p>প্রকৌ. উদাহরণ নাম {i+1}</p></div>)}</div></div></section>}
