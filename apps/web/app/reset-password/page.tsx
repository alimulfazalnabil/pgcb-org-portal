'use client';
import {useEffect,useState} from 'react';
export default function ResetPassword(){const[token,setToken]=useState('');const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[msg,setMsg]=useState('');useEffect(()=>{setToken(new URLSearchParams(window.location.search).get('token')||'')},[]);async function submit(e:React.FormEvent){e.preventDefault();if(p1!==p2){setMsg('দুটি পাসওয়ার্ড এক নয়।');return}const r=await fetch('/backend/api/v1/auth/password-reset/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password:p1})});const d=await r.json().catch(()=>({}));setMsg(r.ok?'পাসওয়ার্ড পরিবর্তন হয়েছে। এখন লগইন করুন।':d.detail||'Reset failed');}return <section className="section"><div className="container narrow"><div className="card card-body"><span className="eyebrow">ACCOUNT RECOVERY</span><h1>নতুন পাসওয়ার্ড</h1><form className="form-stack" onSubmit={submit}><input value={token} onChange={e=>setToken(e.target.value)} placeholder="Reset token" required/><input type="password" minLength={8} value={p1} onChange={e=>setP1(e.target.value)} placeholder="নতুন পাসওয়ার্ড" required/><input type="password" minLength={8} value={p2} onChange={e=>setP2(e.target.value)} placeholder="পুনরায় লিখুন" required/><button className="btn btn-green">পাসওয়ার্ড সেট করুন</button></form>{msg&&<div className="notice-success">{msg}</div>}</div></div></section>}
